[project]
name = "erpl-adt-integration-tests"
version = "0.1.0"
description = "Integration tests for erpl-adt CLI against SAP ABAP Cloud Developer Trial"
requires-python = ">=3.10"
dependencies = [
    "pytest>=7.4.0",
    "pytest-timeout>=2.1.0",
    "pytest-order>=1.1.0",
]
